
// Instantiate all the block 

// Connect them, if there are multiple connections and a select mux has to be placed, create a control signal here, and put the same in control unit 

// get the memory, GPIO, uart, sha256, clock / reset as inputs and outputs

module cpu(
	input clk, 
	input rst, 

	// memory interface 
	output reg [31:0] mem_addr, 
	output reg [31:0] mem_wdata, 
	output reg [31:0] mem_rdata
	

)


// IF stage 

// Instantiate the PC 



// IF / ID pipeline reg 


